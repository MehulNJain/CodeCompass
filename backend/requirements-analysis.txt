# Analysis-pipeline dependencies (modules M1–M7).
#
# Kept separate from requirements.txt because sentence-transformers pulls in
# torch, which is a multi-gigabyte download. Install these only in the
# environment that actually runs the analysis:
#
#     pip install -r requirements.txt -r requirements-analysis.txt

-r requirements.txt

# --- M2 Parsing: structural extraction ---
tree-sitter>=0.23,<1.0
tree-sitter-python>=0.23,<1.0
tree-sitter-javascript>=0.23,<1.0
tree-sitter-typescript>=0.23,<1.0

# --- M3/M4 Graph and ranking ---
networkx>=3.4,<4.0

# --- M6 Semantic: embeddings for grounded Q&A ---
sentence-transformers>=3.3,<6.0
